import { ButtonStyle } from "./styles";

export function ButtonExp({fun,active, name}: any){
  return(
    <ButtonStyle onClick={} className={active} name={name}>
      Ultracargo
    </ButtonStyle>
  )
}